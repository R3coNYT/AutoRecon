# Install necessary packages
sudo apt update && sudo apt upgrade -y && sudo apt install -y nmap masscan python3 python3-venv python3-pip

# Move inside the tool directory
cd ./Autorecon

# Create Virtual Environment
python3 -m venv autorecon_env

# Activate Virtual Environment
source autorecon_env/bin/activate

# Install Dependencies
pip install -r requirements.txt

# Launch the Tool
python3 AutoRecon.py


#########
# START #
#########

source autorecon_env/bin/activate

python3 AutoRecon.py

